name: ci
on: [push, pull_request]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # https://docs.github.com/en/actions/using-github-hosted-runners
        os: [ubuntu-latest, windows-latest, macos-latest, macos-13]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: stable
          
      - name: Install libx11-dev for clipboard support
        if: runner.os == 'Linux'
        run: sudo apt install libx11-dev

      - name: Build
        run: go build -ldflags "-s -w" -o discord-cli .

      - uses: actions/upload-artifact@v4
        if: ${{ github.event_name == 'push' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
        with:
          name: discord-cli_${{ runner.os }}_${{ runner.arch }}
          path: |
            discord-cli
            discord-cli.exe

      - name: Send repository dispatch
        if: ${{ runner.os == 'Windows' && github.event_name == 'push' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
        env:
          GH_TOKEN: ${{ secrets.PAT }}
        run: |
          gh api --method POST -H "Accept: application/vnd.github+json" -f "event_type=discord-cli-ci-completed" /repos/vvirtues/bucket/dispatches
